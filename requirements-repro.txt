# Reproduction pipeline (src/ + scripts/). Versions are minimums known to work;
# training was done on Python 3.11 with an NVIDIA L4 GPU.
torch>=2.1
torchvision>=0.16
timm>=0.9.12
scikit-learn>=1.3
scipy>=1.11
pandas>=2.0
numpy>=1.24
pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
